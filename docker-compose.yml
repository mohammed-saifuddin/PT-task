# docker/docker-compose.yml
version: "3.8"
services:
  yii2:
    image: saifuddin337/yii2-app:latest
    ports:
      - "9000:80"
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
